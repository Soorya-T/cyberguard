# ===============================
# CyberGuard Backend Dependencies
# ===============================
# 
# Authentication + Multi-Tenancy + RBAC
# Production-ready FastAPI backend
#

# -------------------------------
# FastAPI Framework
# -------------------------------
fastapi==0.110.0          # High-performance ASGI web framework
uvicorn[standard]==0.29.0 # ASGI server with WebSocket support

# -------------------------------
# Database
# -------------------------------
sqlalchemy==2.0.29        # ORM for database interactions
psycopg2-binary==2.9.9    # PostgreSQL database driver
alembic==1.13.1           # Database migration tool

# -------------------------------
# Authentication & Security
# -------------------------------
python-jose[cryptography]==3.3.0  # JWT encoding/decoding (jose)
argon2-cffi==23.1.0               # Argon2 password hashing (memory-hard)

# -------------------------------
# Request Handling
# -------------------------------
python-multipart==0.0.9   # Required for form data / OAuth2 flows

# -------------------------------
# Data Validation & Settings
# -------------------------------
pydantic==2.6.4           # Data validation
pydantic-settings==2.2.1  # Environment-based configuration
email-validator==2.1.1    # Email validation for Pydantic

# -------------------------------
# Logging & Monitoring
# -------------------------------
structlog==24.1.0         # Structured logging (optional enhancement)

# -------------------------------
# Development & Testing
# -------------------------------
pytest==8.1.1             # Testing framework
pytest-asyncio==0.23.6    # Async test support
httpx==0.27.0             # HTTP client for testing
pytest-cov==4.1.0         # Coverage reporting for pytest

# -------------------------------
# Security Headers
# -------------------------------
# Note: Security headers are handled by custom middleware
# No additional packages required

# -------------------------------
# Reporting (A3)
# -------------------------------
jinja2==3.1.3
weasyprint==61.2
# -------------------------------